import turtle as t

t.pensize(20)

# 设置颜色模型（取值范围是0~255）
# t.colormode(255)
# t.pencolor(89, 74, 49)

# t.pencolor('#594A31')

# 设置颜色模型（取值范围是0.0~1.0）
# t.colormode(1.0)
# t.pencolor(89 / 255, 74 / 255, 49 / 255)
t.colormode(255)
t.pencolor(89, 74, 49)

t.circle(100)

t.mainloop()